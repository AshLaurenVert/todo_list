<tr>
<td><%= task.description %></td>
<td><%= task.deadline %></td>
<td><%= link_to "Edit", edit_task_path(task), class: 'btn btn-info', remote: true %></td>
<td><%= link_to "Delete", task, class: 'btn btn-danger', remote: true, method: :delete, data: {confirm: 'Are you sure?'} %></td>
</tr>
